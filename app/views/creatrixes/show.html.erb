<div class="text-center holy"> <h1> My account </h1> </div>

<div class="row">
  <div class="col-12 col-md-4 float-lg-right">
    
    <div  id="flex-profile" class="sticky">
    <%if @creatrix.avatar.attached?%>
    <a data-toggle="collapse" data-target="#collapse" href="tollapse" role="button" aria-expanded="false" aria-controls="collapseExample">
      <div><%= image_tag @creatrix.avatar, alt: 'avatar', class: "img-fluid", id: "avatar-image" %></div>
    </a>
    <%end%>
    
    <div class="collapse"  id="collapse">
      <%= form_tag creatrix_avatars_path(@creatrix),  multipart: true do %>
      <p class="text-center"> Update your profile picture </p>
      <div class="text-center"> <%= file_field_tag :avatar, class: "btn btn-secondary" %> </div>
      <div class="text-center"> <%= submit_tag "Update", class: "btn btn-secondary" %> </div>
<% end %>
    </div>

        <div><h2><%= @creatrix.itself.hexanyme %></h2></div>
      <p-lead class="lead"></p-lead>    
      <% if @creatrix == current_creatrix %>
        <div id="creatrix-details d-flex">
          <h4 class="mt-3 mb-1 text-center">My details: </h4>
          <p class="text-center"><strong> Email address </strong>: <%= @creatrix.email %></p>
          <div> <%= link_to "e d i t", edit_creatrix_registration_path, class: 'btn btn-secondary mb-4', id:"button-profile" %>  </div>
          <div> <hr class="solid holy"> </div>
          <div> 
            <a href="#microservices" class="btn btn-primary mt-3 holy" id="button-profile">Microservices > </a>
            <a href="#requests" class="btn btn-primary mt-3 holy" id="button-profile">My requests > </a>
            <a href="#nodes" class="btn btn-primary mt-3 holy" id="button-profile">Digitales > </a>
            <a href="#donations" class="btn btn-primary mt-3 holy" id="button-profile">Donations > </a>
          </div> 
        </div> 
    </div>
    <% end %>
  </div>
  <div class='col-12 col-md-8 padding-creatrixes'>
    <div class='text-center' id="microservices">
    <h3 class="text-center holy mb-4">m i c r o s e r v i c e . s</h3>
    <i>Offer your services to node creator by uploading a demo media and offering a price</i>
    </div>
    <%= render "microservices/new"%>
    <div class='text-center' id="requests">
      <h3 class="text-center holy padding-creatrixes">r e q u e s t . s</h3>
      <i>You will see here the the requests from the node creator for you realise a work for their node.</i>
    </div>
    <div class="d-flex flex-column holy padding-creatrixes">
      <% @microservice_requests.each do |microservice_request| %>
        <%= render "microservice_requests/card", microservice_request: microservice_request%>
      <%end%>
    </div>
    <div class='text-center holy' id="nodes">
      <h3 class="text-center padding-creatrixes mb-3">d i g i t a l e . s</h3>
        <i>You will see here the nodes that you've created.</i>
      <div id="flex-digitales">
        <% @digitales.each do |digitale|%>
          <div class="gg-pool-card-secondary" style="height: 200px; width: 210px"> 
              <%= link_to digitale_path(digitale.id), class: "d-flex flex-column" do %>
                <i class="fas fa-hand-holding-usd mb-3"></i>	
                creep in 
              <% end %>
          </div>
        <%end%>
      </div>  
    </div>

    <% @creatrix.nodes.each do |node| %>
      <h3><%= link_to node.title, digitale_path(node.digitale.id)  %></h3>
      <h6><%= node.achievements.count.to_s %> achievements out of 21</h6>
      <p><%= node.content %></p>
      <p></p>
    <% end %>

    <div class='text-center holy' id="donations">
      <h3 class="text-center padding-creatrixes">d o n a t i o n . s</h3>
      <i class="mt-3 mb-3">You will find here all the donations that you've made:</i>
          <p class="mt-3 mb-3"> You made <%= @donated_sums.length%> donations</p>


      <div id="flex-digitales">
        <% 8.times do%>
          <div class="gg-pool-card-secondary" style="height: 250px; width: 210px"> 
            <a class="panel-title px-4 text-center" data-toggle="collapse" data-target="#collapse1" href="tollapse" role="button" aria-expanded="false" aria-controls="collapseExample">
            <h5>  Digitales </h5> </a>
            <div class="collapse px-4 text-center" id="collapse1">
              <div> <p> Description </p> </div>
            </div>
            <div></div>
            <div> <h3>Amount</h3></div>
              <%= link_to nodes_path, class: "d-flex flex-column" do %>
                <i class="fas fa-hand-holding-usd mb-3"></i>	
                creep in 
              <% end %>
          </div>
        <%end%>
      </div>  

    </div>

    <%@donated_sums.each do |donated_sum|%>
      <h4><%=donated_sum.fundraiser.microservice_request.achievement.node.title%></h4>
      <p><%=donated_sum.fundraiser.microservice_request.achievement.node.content%></p>
      <div><%= link_to "Go to node", digitale_path(donated_sum.fundraiser.microservice_request.achievement.node.digitale.id), class: "btn btn-secondary"%></div>
    <%end%>

  </div>
</div>