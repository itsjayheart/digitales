<div class="text-center holy"> <h1> My account </h1> </div>

<div class="row">
  <div class="col-12 col-md-4 float-lg-right">
    
    <div  id="flex-profile" class="sticky">
    <%if @creatrix.avatar.attached?%>
      <div><%= image_tag @creatrix.avatar, alt: 'avatar', class: "img-fluid", id: "avatar-image" %></div>
    <%end%>
        <div><h2><%= @creatrix.itself.hexanyme %></h2></div>
      <p-lead class="lead"></p-lead>    
      <% if @creatrix == current_creatrix %>
        <div id="creatrix-details d-flex">
          <h4 class="mt-3 mb-1 text-center">My details: </h4>
          <p class="text-center"><strong> Email address </strong>: <%= @creatrix.email %></p>
          <div> <%= link_to "e d i t", edit_creatrix_registration_path, class: 'btn btn-secondary', id:"button-profile" %>  </div>
        </div> 
    </div>
    <% end %>
  </div>
  <div class='col-12 col-md-8 padding-creatrixes'>
    <div class='text-center'>
    <h3 class="text-center holy mb-4">m i c r o s e r v i c e . s</h3>
    <i>Offer your services to node creator by uploading a demo media and offering a price</i>
    </div>
    <%= render "microservices/new"%>
    <div class='text-center'>
      <h3 class="text-center holy padding-creatrixes">r e q u e s t . s</h3>
      <i>You will see here the the requests from the node creator for you realise a work for their node.</i>
    </div>
    <div class="d-flex flex-column holy padding-creatrixes">
      <% @microservice_requests.each do |microservice_request| %>
        <%= render "microservice_requests/card", microservice_request: microservice_request%>
      <%end%>
    </div>
    <div class='text-center holy'>
      <h3 class="text-center padding-creatrixes">n o d e . s</h3>
      <i>You will see here the nodes that you've created.</i>
    </div>
    <% @creatrix.nodes.each do |node| %>
      <h3><%= link_to node.title, digitale_path(node.digitale.id)  %></h3>
      <h6><%= node.achievements.count.to_s %> achievements out of 21</h6>
      <p><%= node.content %></p>
      <p></p>
    <% end %>
    <div class='text-center holy'>
    <h3 class="text-center padding-creatrixes">d o n a t i o n . s</h3>
    <i>You will find here all the donations that you've made</i>
    </div>
    <p>you made <%= @donated_sums.length%> donations</p>
    <%@donated_sums.each do |donated_sum|%>
      <h4><%=donated_sum.fundraiser.microservice_request.achievement.node.title%></h4>
      <p><%=donated_sum.fundraiser.microservice_request.achievement.node.content%></p>
      <div><%= link_to "Go to node", digitale_path(donated_sum.fundraiser.microservice_request.achievement.node.digitale.id), class: "btn btn-secondary"%></div>
    <%end%>
  </div>
</div>