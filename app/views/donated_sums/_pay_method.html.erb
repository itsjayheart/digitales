<%= form_with model: donated_sum, method: "get", url: new_node_donated_sum_path(@node.id), local: true do |f| %>
  <%= hidden_field(:donated_sum, :sum, :value => sum) %>
  <%= f.submit "Pay with stripe" , class: "m-2 btn btn-lg btn-success" %>
<%end%>